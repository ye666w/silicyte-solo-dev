# 2026-09-24 — root

Landed on silicyte main: `e7e4d8d..6eeffe7`. It fixes the four environment problems the operator's
release fleet ran into (his answer to task d748701d):

1. **Parking killed background tasks.** An idle session with non-ambient background work is no
   longer parked, and a restart for rewritten skills waits for that work to finish. In this CLI,
   `ambient` means skipTranscript housekeeping or an ambient monitor_ws watcher. A Monitor-tool
   watcher is a local_bash monitor, so it counts as work. A park that cannot wait (fleet restart,
   config relaunch, root recovery) writes `backgroundWorkLostWithItsProcess` to the registry row.
   The session is told once when it comes back. After a restart, start.ts wakes it to hear this.
2. **Subagents.** `maySpawnSubagents` lifts the ban on Agent and Task only. SendMessage and
   RemoteTrigger stay refused for every role. RemoteTrigger was open before this and creates
   cloud routines. src/subagent-guard.ts refuses `isolation: 'remote'`.
3. **Workspace pull.** The write guard lets `pull --ff-only` and `merge --ff-only` through as an
   allow-list of flags; -c and --config-env before them are refused. Fixed along the way:
   `fetch +src:dst`, and git global options whose separate-word value was read as the
   subcommand, which let the guard fail open.
4. **Skill edits.** apply_skill_changes fingerprints skills by skill name and covers visibleSkills
   and roles whose skillName differs from the role name. Affected sessions restart. Those that
   see the skill are owed a notice to read it again, prefixed onto their next message.

Still open, not started:
- a classifier stop opens no incident: bus.ts maps only model_refusal_fallback and
  model_refusal_no_fallback;
- `git config` in the workspace passes the guard, so repo config could turn autostash back on;
- a subagent's compact_boundary may be read as the parent's compaction (unverified);
- ListAgents is not refused. It is read-only.

Workers, parked and warm:
- 44189bb9: skills, apply_skill_changes;
- b6a202ea: write-guard git parsing;
- a4fa2fc0: subagent tools and guard.

## Later: the session panel opens on the newest messages (efc928d)

The operator's answer to 6b22682b: a session panel should open already scrolled to the newest
messages. Cause: renderTranscript scrolled to the bottom only if the transcript it replaced was
at the bottom, and it measured after renderComposer. Going from a worker to root opens the
composer, which shortens the transcript past the 60 px band. A closed panel is display:none and
loses its offset. Now showSession passes justOpened (a different sid, or the panel was not on a
session), and an opening always ends on the newest entry. Repaints of the same session keep the
old rule. Three tests in panel-behaviour, mutation-checked both ways. The fleet was restarted
onto efc928d.

## Later: a paused fleet no longer counts as quiet (80c0349)

The operator's next answer to 6b22682b: he stops the fleet to think over a long message of mine.
On Resume the idle nudge fired within 10 s, ahead of his answer. Cause: nudgeIfEverythingIsQuiet
measures from lastActivityAt, which nothing moved during the freeze. Now unfreeze() resets it when
anything was unfrozen. That covers Resume, lifting a halt (a halt freezes the fleet too), incident
verdicts and the limit lifting. Test in idle-nudge, mutation-checked. The fleet was restarted onto
80c0349. Question 6b22682b is answered twice over; the older options (classifier incident, git
config, subagent compaction) are still open, unasked since.

## Declined: the autostash follow-up

The operator declined both versions: refusing `git config` in the workspace, and requiring
--no-autostash on workspace pull/merge. His words: "не делаем это, выглядит подозрительно". Do not
propose it again. For the record, a git 2.54 experiment showed autostash can also be switched on
by GIT_CONFIG_COUNT/KEY/VALUE and GIT_CONFIG_GLOBAL, which the guard does not see; he decided
knowing that. Still open: a classifier stop opens no incident; a subagent's compact_boundary may
count as the parent's (unverified).

## Not a bug: "a classifier stop opens no incident"

This is by design (supervisor.ts, handleRefusalThatChangedNoModel). A single model_refusal_no_fallback
tells the session to find another way and notifies the operator. An incident opens on a downgrade
(model_refusal_fallback), or after REFUSALS_BEFORE_TREATING_IT_AS_AN_INCIDENT (3) within
REFUSAL_STREAK_WINDOW_MS (15 min) in one session. The only real stop in the activity log was root on
Sep 23, 02:44, category cyber; it went exactly that way and recovered in 40 s. Listing it as open was
my misreading. Proposed to the operator in 6b22682b: close it, or make the threshold a setting.

## The classifier threshold is a setting now (9300be5)

The operator chose "make it a setting" ("ок, делаем"). New timings: incidentAfterRefusals (default 3,
range 1-20) and refusalStreakMinutes (default 15, range 1-1440). Neither can be switched off. The
supervisor reads them in handleRefusalThatChangedNoModel; the two constants are gone. Labels are in
en/ru. Four tests in refusal.test.ts stub incidents.open with a recorder. The three setting tests
fail on the old constants, and the default test passes on both. The fleet was restarted onto 9300be5.
Still open: whether a subagent's compact_boundary counts as the parent's (unverified).

## Not a bug: "a subagent's compact_boundary may count as the parent's"

The operator asked what this item meant (6b22682b), so I checked it before explaining. Verified in
the CLI the fleet runs (claude-agent-sdk 0.3.280, the darwin-arm64 binary): the Agent tool hands
every message its subagent produces to one onMessage handler, and that handler passes on to the
parent only api_error (turned into agent_api_retry progress), bash/powershell progress, nested
agent/skill progress (only with forwardSubagentText), and the subagent's assistant/user messages
(tool_use/tool_result blocks only, unless forwardSubagentText). Every other system message,
compact_boundary and status included, is dropped before the parent's stream. silicyte never sets
forwardSubagentText, and it would not pass compact_boundary anyway. So compaction.compacted() only
ever hears the session's own compaction. Nothing to fix. Re-check only after an SDK upgrade.
Nothing of mine is open now.

## Browser notifications (9300be5..f742362)

The operator asked for browser notifications (6b22682b). Worker b6a202ea built them to my spec, then
made four fixes from my review. The browser is a fourth channel for the same three NOTIFIABLE events
(operatorTask, incident, fleetStopped). notifyHuman with an event puts humanNotified on the bus
before awaiting Slack and Telegram; the panel gets it through the existing /api/events 'event'
frames, and the activity log ignores it. src/web/panel-notify.ts keeps this browser's choices in
localStorage (all three on until he changes one), checks permission and secure context, and shows a
notification only when the panel is hidden or unfocused, tagged event-at so two tabs give one.
Clicking it focuses the tab, closes it, and opens the questions view for operatorTask. The "Browser"
card sits first in Settings, Integrations: permission state, an Allow button, the three checkboxes,
and a test button whose message points at macOS System Settings when nothing appears. Limits, told
to the operator: it needs an open panel tab (Web Push would lift that, a bigger job) and localhost
or https. 893 tests green; the harness stubs Notification.

Confirmed in a real browser by the operator the same night. His first test said "Sent" and nothing
appeared; the cause was macOS notification settings or Focus, not the code, and after he fixed that it
works. "Sent" means the page's part is proven: permission granted and the notification accepted, with
no focus check and no tag on the test. When it is "Sent" and nothing shows, send him to Notification
Center, Focus, and System Settings, Notifications, his browser, before reading any code.

## Jev removed from the root skill

The operator saw 812,726 tokens on the TypeSafe dashboard. From this fleet's transcripts: 28 requests,
all from root sessions (22.09: 9, 23.09: 19, UTC), 23 answered with 44 questions, and Jev's own usage
fields add up to 16,695 input and 842 output tokens. The gap of about 46 times is unexplained: either
the key is shared, or billing counts far more than the response reports. No answer I remember changed
a decision. At his word the gate section, the fast-jev-compaction paragraph and the operator-section
gate paragraph are cut from skills/root/SKILL.md; the worker skill and the product never mentioned
Jev. Do not bring it back without him.

## Possible sessions on the graph (f742362..ff14da0)

The operator asked for every possible session on the graph, branched as the config says, clearly not
running. Worker b6a202ea built it to my spec in two rounds. src/possible-sessions.ts is the one place
the rule lives: for each live session, per role in mayOwnRoles, count = min(maxOwnedAtOnce minus
not-stopped direct reports, maxPerRole minus live of that role, sessionCap minus the capped count),
nothing past maxTreeDepth, wrapper-owned and undeclared roles skipped, an invalid maxPerRole gives 0.
Each position carries what one of it could start in turn, with the ghosts above it counted as taken;
64 positions at most per fleet state. With no human entry point the managerRole is a ghost root.
possibleSessionsForFleet derives the inputs from the registry, and server.ts only calls it; fleetState
carries possible on tree nodes and possibleRoot. Drawn as one node per position, not per slot: class
agent possible, dashed faint shell, a stack of shells for more than one, "×3 · not started", dashed
edge, no click, never selectable; a ghost root hides "Fleet is empty". The drift guard runs the real
fleet_request (quota, role, depth) and refusalIfTheRoleIsFull and the cap against the same registry.
Soft one-per-role rule, account hold and freezes are deliberately ignored: it shows structure. Offered
him one ghost per slot instead of ×N if he prefers; the data already supports it. 917 tests green.

## Attachments: files and images in both directions (asked 24.09, in progress)

The operator wants to paste screenshots and send files, and wants sessions to show him images and files, "so our chat is not inferior to Claude's". Decided shape: upload first, send by name, as Claude's chat does.

- Phase 1, operator to session (worker 44189bb9): src/attachments.ts stores flat content-addressed files <sha256 24 hex>.<ext> with a <hash>.json sidecar (originalName UTF-8, mediaType, size, kind, at) under the .silicyte state dir. POST /api/attachments takes raw bytes plus X-Attachment-Name; kind is image only by magic bytes (PNG, JPEG, GIF, WebP), SVG is a file; image 5 MB, file 50 MB. /api/human-message and question answers take attachments: names, at most 10. Delivery: image blocks first, text last, plus one "[attached «name», size, type: /abs/path]" line per attachment so a session can Read a PDF or forward a path. No attachments: content stays the plain string. GET /api/attachments/<name>: images inline, everything else octet-stream as a download, nosniff, CSP sandbox, behind the auth gate: an HTML or SVG served inline from the panel origin would act with the operator's authority.
- Phase 2, session to operator (44189bb9 after phase 1): ask_operator gets attachments (absolute paths), a new share_with_operator tool for the human-facing roles, files imported into the same store. Confused-deputy guard: realpath inside the session's own worktree or the shared TMPDIR, regular file, nlink 1, mustNotRead respected, same size limits. Without it a session could copy a file it may not read (~/.ssh, integrations.json) into a directory it can read.
- Panel (b6a202ea after its screenshot job): paste, drop and file picker, upload on attach with progress, thumbnails and file chips, lightbox, both directions in the conversation and in tasks.
- Client side: images above 2576 px on the long edge (the current models' maximum, 3.75 MP) are scaled down before upload; nothing below that is touched.

Visual redesign is paused behind this: b6a202ea is rendering the panel to PNG with headless Chrome on fixture data. Measured already: frozen #f5a524 and supervisor #e0a33e are ΔE 3.7 apart, starting #4a9eff and accent #7c8cff ΔE 6.2 (1.4 for deutan), stopped #3a4049 is 1.7:1 on the panel surface.

Landed ff14da0..63d211b: attachments both ways (57923ea phase 1, 0238141 five fixes, cfc39d1 phase 2,
f71cf0b panel, 63d211b race fix). Question attachments live on each exchange: attachments (the
asker's) and answerAttachments (the operator's), summaries in /api/questions. My own review found one
real hole the worker's guard left: realpath was checked before the open, and O_NOFOLLOW covers only
the last component, so a directory swapped for a symlink in that gap had the supervisor read ~/.ssh
into a readable place; a test that does exactly that swap read the secret before the fix. Now the
path is resolved again after the open and the inode compared. The panel has no auth of its own, only
the Host check (and Origin for non-GET), so img src works without blobs. Rendering the panel from a
session is impossible: bind() is denied even for Unix sockets (Chrome's ProcessSingleton) and
QuickLook cannot nest its sandbox. Next: the new theme (design-theme-new.md), then chat functions.

## The new look (25.09, past midnight)

No Classic: the operator said so, one :root token layer replaces the old look. Landed da241f7 (phase A:
tokens, sans for interface and mono only for code and ids, one button system, flat nodes, reduced
motion) and ac3742f (B1: panel-icons.ts, a node labelled by its short sid with role · model · status
under it, meters with the stop and compaction ticks from fleetState budgetStopThresholds and
contextCompactAtPercent; the favicon the operator asked for, the graph's flat node whose ring follows
the fleet, with a dot while a question waits). B2 is with b6a202ea: author lines with time, the
session's text unboxed, the operator's bubble on the right, agent-to-agent cards with arrows, the
supervisor card, the docked composer toolbar, alerts and empty states. After B2, the chat functions,
in this order: markdown, copy buttons, drafts per session, a view of tool calls and thinking,
streaming text. The operator sends screenshots through attachments; no session can render the panel.

Corrected by a419a81: the short sid over the node and the role · model · status caption under it ran
into each other three workers wide, and the operator asked for the role alone. A node now reads its role
and nothing sits under a live node; sid, role · model · status and tokens live in the tooltip. The
ghost's "×N · not started" and the aside's one-word captions stay. Graph text wider than a column
collides: one short word per node.

## Lost in the restart (00:28)

The operator's Timings screenshot (queued 00:26:18) and b6a202ea's cut-short report both show as
"message → root" at 00:28:32, the second root's turn was cut for fleet_restart. Neither reached root's
conversation after the resume, but the panel shows both as delivered. 44189bb9 has the fix (sent 00:34):
cause, a failing test, a message kept until it can be delivered. Hold restarts until it lands; then
restart once for it, 18d87b8, and B2 if ready.

Landed 18d87b8 from that screenshot: timings unit "%" (the name says of what), units min-width 6ch so
the inputs line up, the panel head's h2 and the timings inputs in sans, formatTokens reads B from 999.5M
(the root read "4206M tokens").

For the B2 review, from the operator's 00:29 screenshot: an attachment-only message from him draws an
empty bubble; detail.hypervisor still carries a text ⚙ in both locales; a collapsed agent card shows one
faded line of raw "[report from …]".

Landed 4c8220b at the operator's word: the tag row collapses to in/out/cache on one line (repos, session
id, reports-to, connectors behind +N), no role tag at all ("the graph and the panel already name it"),
detail.root gone, a frozen reason on its own line. 00:40 the fleet stopped at five-hour 95%; the
operator resumed it himself at 00:42 (94%, resets 02:30). Both workers re-sent at 00:43: b6a202ea B2
plus the three review items above; 44189bb9 the lost-message fix, with the budget stop as one more path
to check. Restart still held until that fix lands: then one restart for 18d87b8, 4c8220b, the fix, B2.

02:30 the five-hour window reset; both workers re-sent 02:36. 02:40 B2 landed as ab8eab3 (86c0f2e the
conversation, ab8eab3 the three screenshot items), 1040 tests green. Reviewed the scripts: a peer reads
"worker · 02ea" (sid.slice(-4)), which matches server.ts and the memory rows, so it is house style, not
a defect. The restart waits for 44189bb9's lost-message fix (failing test 5cae08d on its branch, the
fix in progress); then one restart for 18d87b8, 4c8220b, ab8eab3 and the fix.

02:50 landed the lost-message fix (622e6ae test, f4ceaa0 fix, from 44189bb9). Cause: park() drops the
handle before the trailing 'result'; that result drained the queue into send(), which woke a fresh
process and reported messageSent, and process.exit took both. Now send() holds on the registry row
(heldSends, persisted) while theWholeProcessIsAboutToExitForARestart, and unpark() flushes. Two gaps
still open for 44189bb9 after the restart: (A) start.ts shutdown() also calls parkEverythingForShutdown
then exits, without the flag, so set it inside parkEverythingForShutdown; (B) after a start nothing wakes
a parked worker holding heldSends (only root is resumed), so wake them at startup as
wakeWhoeverLostBackgroundWork does. Known: a turn ended by the account's hard limit reports "success"
(bus.ts, subtype is success and the limit text is the result); left for later. This restart still runs
on the old code, so it goes only when nothing is mid-turn and nothing is queued for root.

02:45 restarted onto f4ceaa0: the new conversation, timings, tags and the held-message fix are live.
Everyone came back. 44189bb9 has gaps (A) and (B) (02:46). b6a202ea, compacted at 02:47, has the
first three chat features: a hand-written, escape-first markdown renderer with http/https/mailto links
only, copy buttons on code blocks and on messages, and a composer draft per session in localStorage.
Tool calls, thinking and streaming come after that and need the server side.

Lesson: activity.log lines carry a time and no date, so grepping by time mixes days. At 02:45 that read
as three roots all called root-fe9b8f71 working at once. Anchor a search on a line only today could
have written, or look at the file's tail.

02:58 landed 8d70aad (from 44189bb9's f3ba533): the flag is theWholeProcessIsAboutToExit, also set in
parkEverythingForShutdown, and start.ts wakes every parked session holding heldSends, root exactly once.
Live from the next restart; no restart for it alone. Still open and narrow: a start while frozen, halted
or limit-held skips that wake, so the held message arrives with the next send. 44189bb9 is now
researching the server side of tool calls, thinking and streaming, as a report only with no code.

03:05 all three workers are busy. 44189bb9 is building the server side of tool calls and streaming from
its research: bus.ts drops tool_use, tool_result, thinking and stream_event today; thinking text is
empty (signature only) under the current config. The plan is one stored 'tools' entry per run of calls
(cap 200, one write at close), deltas SSE-only, includePartialMessages on, and nothing in
src/web/panel-*. Nothing lands until b6a202ea's panel side can render it: an unknown entry kind would
crash entryElement on entry.text.trim(). a4fa2fc0 is cleared and retuned to Sonnet (its subagent
work landed as 896a7a7 and c8d45e6) and is researching how thinking becomes readable and at what
cost; that decision goes to the operator. The SDK's d.ts lives in the orchestrator checkout's
node_modules; worktrees have none.

03:15 a4fa2fc0's finding, checked against sdk.d.ts (thinking?: ThinkingConfig at line 1838, display?:
'summarized' | 'omitted' at 9304/9325): thinking is empty because display defaults to "omitted" on
Opus 5.5 and Sonnet 5. thinking: {type: 'adaptive', display: 'summarized'} gives a summary, never the
raw chain. Billing is the same; the first answer text comes later. It can be set per session.
redacted_thinking never has text. {type:'enabled', budgetTokens} is rejected on these models. Asked the
operator (3614cbb0): all sessions, root only, or off. When he answers, thinking goes to 44189bb9 as
round 2, once the tool-call and streaming report is in.

03:35 landed b6a202ea's markdown, copy buttons and per-session drafts (a19aa16, fc406cf, 4db2cb5) with
my review fixes in 4e92852. Lists dropped every item after an unexpected dedent. A send that finished
after a switch cleared the open session's draft. A second copy kept "copied" as the title. Both
locales had been re-indented to 2 spaces; the house format is json.dumps indent=1. 44189bb9's server
side (4805f2b bus ingest, 593d2d0 server folding and ?sid= SSE filter) is reviewed and waits on its
branch for the panel side. Contract: toolCall/toolResult fold into {kind:'tools', id, calls[], moreCount?},
upserted by id over SSE 'entry'; textDelta {sid, messageId, text} is SSE-only; the final 'text' event
carries messageId. At integration, keep textDelta out of the bus's 5000-event replay history (only the
session endpoint's `events` reads it, and the panel ignores them). Restarting now, since nobody is
mid-turn; then b6a202ea builds the panel side on top of 44189bb9's two commits. Thinking waits for
3614cbb0.
