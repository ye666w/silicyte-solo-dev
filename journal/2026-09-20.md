# 2026-09-20

First day of this fleet. Nothing above this line exists — the nine-role fleet that came before
was removed today, and its record lives in `ye666w/silicyte-dev-team`, not here.

## What this fleet is

One root on Opus 5 at max effort, six workers on Haiku. `maxSessions: 7`, rails from
`origin/main`, product at `/Users/photon/projects/silicyte/silicyte`.

The reason is arithmetic, and it is written up in `workspace/RESEARCH.md` under Money: 97–99% of
what any session spends is re-reading its own context, and this session re-reads at fifteen times
what a worker costs. Workers are not hands. They are a way of not putting material into the
context that costs the most to hold. Everything else in `skills/root/SKILL.md` follows from that.

## How I got here

I was the operator's session outside the fleet, and I was seated as root by
`workspace/transplant.sh` — a registry row carrying my `claudeSessionId`, then `npm start`. The
conversation came across whole and the root skill was applied fresh on top of it. Both halves of
that were measured first, not assumed, and both are in RESEARCH.md under "Resuming a session":

  `resume` ignores `cwd` — the transcript stays in the project directory it was created under.
  `systemPrompt.snapshot: false` is what lets a resume re-brief a session. `true`, and the
  omitted default, freeze the prompt at creation. silicyte passes `false` for every role but
  Guardian, which is also what makes `apply_skill_changes` work at all — and nothing tests it.

## Where things stand

No workers running. This is the fleet's first turn.

**The weekly window is at 83% and `stopFleetAtWeeklyPercent` is 90.** It resets 22.09 at 14:00
+05. Seven points of headroom for two days, shared with the operator's own work outside the
fleet. Check `account_limits` before starting anything long; if it trips, the fleet stops itself
and comes back on its own, and that is not a failure needing rescue.

The five-hour window is fresh (0%, resets 20:00 +05).

## Answered, and done

**Resume during a limit hold.** Task `804424f6`. The operator split it in two, and the split was
the thing I had missed: Resume after *our own ceiling* stopped the fleet means "I know, spend the
rest of the window" — clear the deadline and do not re-arm; Resume when *the account itself is
out* means nothing, so keep the deadline and let the fleet wake itself at the reset.

I had recommended re-arming the guard. That was wrong and the operator overrode it: a guard that
re-arms stops the fleet again within seconds and makes the button a lie. `spendingThisWindowOutUntil`
holds the ceiling off for that window only, so it returns with the next one.

Landed as `1f9dbac` on `silicyte/root-5cf1dd86`: both spawn guards moved onto the synchronous
decision, a refusal by the account became a stop cause in its own right, Resume splits by cause.
430/430 green, typecheck and lint clean. Full write-up in RESEARCH.md under "Rate limits and the
stop"; the old soft-spot entry is now the closed one, as that file's rule requires.

Worth keeping: step one alone — moving the spawn guard — turned
`tests/stop-at-the-limit.test.ts:261` red. That test encodes the operator's own intent for the
ceiling case, so it was not a test to invert but the thing to restore. Running the suite between
steps is what caught it; had all three landed in one edit I would have read the green as
confirmation and never seen that the middle state was wrong.

## A lesson about my own task-writing, not about the worker

I sent `worker-a36f9e40` to map which assertions break, and wrote "read the tests, do not run
them; nothing is implemented yet so they all pass." Its answer to that part was **wrong** — it
reported that nothing breaks, reasoning that the old and new guards agree "when both are set by
the normal flow", which is true and misses the only case that matters, the one where `Resume`
clears one and not the other. I had already measured a red test by then.

The worker's own skill tells it to run things. My task overrode that. **When the question is what
breaks, the answer comes from running, and a task that forbids running has answered a different
question.** If a change is not implemented yet, the useful instruction is to implement the
smallest version and run, or to say plainly that the answer is a prediction.

What it did get, and what paid for the whole round, was one line: **no test anywhere uses
`status: 'rejected'`** — the `reading()` helper hardcodes `'allowed'`, so every test of the limit
machinery was a test of the percentage comparison and the entire account-refused path was
untested. That is a fact, checkable in seconds, and it is why the gap survived. Its list of
missing tests also overlapped three of the four I had derived independently, which is a decent
cross-check on both of us.

Shape of the round: large input, small answer, and the small answer was worth it. The part that
went wrong went wrong in my prompt.

## What I am actually here for, and the finding it led to

The operator corrected the premise: this fleet is not here to work a board. It is here so the two
of us make the architecture not rubbish, and `workspace/RESEARCH.md` is where that work is kept.
Its soft spot #5 is one I wrote myself and labelled "the highest-yield audit available here and
has not been done" — ~25 private fields of per-session coordination state in `supervisor.ts`,
each a Set or Map keyed by sid, each added in one place and removed by hand somewhere else.

It has stopped being a hypothesis. Three confirmed instances of the same shape, all in that file:
`af2b817` (the set filled after an await), `1f9dbac` (the same set empty in two situations), and
`compactionRequested` (deleted on one event only, stuck forever after a cancelled compaction).

**Verified by reading the two methods, not inferred.** Seventeen of those collections are keyed by
sid. `closeOne()` :509 — the one place a session ends for good — erases **five** of them:
`humanOutbox`, `reportsHeldUntilUnfreeze`, `clearWhenTheTurnEnds`, `spentBeforeTheCurrentProcess`,
`toldTheOperatorItReadsLow`. `clear()` :1050 erases **one**: `clearWhenTheTurnEnds`. And `clear()`
is the sharper case, because the sid survives while the conversation is thrown away, so every
entry still keyed by it is now about a conversation that does not exist.

So the defect is not any one field. **There is no single place that says "this sid is finished,
forget it."** The registry owns the lifecycle; seventeen side-tables each have to remember on
their own. That is a defect generator, and adding an eighteenth field is how the next one arrives.

Three workers are sweeping all seventeen — add sites, erase sites, which lifecycle moments reach
an erase, and what breaks when the erase never fires. Whether "eleven are never cleaned on close"
holds is theirs to settle; what is above is what I checked myself.

## Two findings verified myself, before the audit is finished

**Landed work never reaches a new session.** Sessions land by `git push origin HEAD:main`, which
moves the remote. Worktrees are cut by `Worktrees.create` from the product checkout's **local**
`main`, which that push does not move. No session may move it either — the write guard refuses
any git subcommand that would write a checkout the fleet reads from, and it is right to. Nothing
in the product does it. So the local `main` advances only when a human runs git in that
directory. Measured, not reasoned: `origin/main` at `b6f961a`, all six worker worktrees at
`af2b817`, and three of six audit reports were consequently written against code one commit stale.
The guard's own refusal text says a change "reaches it from a worktree of its own" — true for the
remote, and it leaves the local checkout behind forever.

**`clear()` throws the conversation away and keeps every judgement formed about it.** This is the
sharp version of soft spot #5, and it is not what I first hypothesised. Cleanup is not organised
by lifecycle event at all; it is organised by *process exit*. `clear()` routes through
`detachFromItsProcess` :952, so everything process-scoped is cleaned — `idleSince`,
`backgroundTasksBySid`, the handle. Everything conversation-scoped survives, because nothing in
the code distinguishes the two:

    compactionRequested        a cancelled compaction disables it for the new conversation too
    refusalsBySid              old refusal timestamps count toward the new conversation's streak
    toldTheOperatorItReadsLow  the drain ends with comesBack=true, so :401 never fires
    humanOutbox                a queued human message is delivered into a conversation that
                               has no idea what it refers to
    reportsHeldUntilUnfreeze   same

So the missing idea is not "a cleanup function". It is that **process identity and conversation
identity are the same `sid`**, and the code has no way to say which of the two a piece of state
belongs to. Add a field and you are silently choosing one by accident.

**The cost of an edit, measured.** `supervisor.ts`: 47 private fields, 112 methods, one class.
`registry` is touched by 62 of those methods, `bus` by 29. Eighteen methods touch four or more
distinct fields; `nudgeIfEverythingIsQuiet` touches eleven. There is no module boundary, so the
only honest way to change anything is to read the file. That is the token bill on every edit, and
it is also why the criticals cluster here rather than anywhere else.

## Durability and the panel, both verified by hand after the reports

**`registry.json` is the only critical state file written non-atomically.** `spending.ts:112` and
`operator-questions.ts:194` both write a half-written temp file and `renameSync` it into place.
`registry.ts` calls `writeFileSync` straight at the destination. So the pattern exists in this
codebase, was applied twice, and was not applied to the file whose loss costs the most: a write
interrupted mid-flight leaves truncated JSON, `loadPersisted()` :140 swallows the parse error and
returns `[]`, and the fleet comes up believing no session ever existed. Silent, total, and a
one-line fix by copying code already in the repo. `timings.json`, `connectors.json` and
`integrations.json` are also non-atomic but all three are cheap to recreate.

**The panel's escaping is sound, and that is a finding worth recording as a negative.**
`renderMarkdown` :793 calls `escapeHtml` **first** and applies the markdown substitutions to the
already-escaped string, which is the correct order — the dangerous version does it the other way
round. Eleven `innerHTML` sites, every one either escaped or fed a string built from escaped
parts. So despite being 2683 lines, `app.html` is not where the risk is, and it does not belong
near the front of any rebuild plan. Its real problem is different and smaller: `renderGraph` is
called on every state event with no debounce and rebuilds the whole SVG, which is invisible at
nine sessions and would not be at a hundred.

One worker claimed the file cannot be split without a build step "because no modules are
available". That is wrong — `<script type="module" src="./x.js">` needs no build step, and the
page uses a single plain `<script>` today purely by choice. The constraint is no *build*, not no
*modules*. Worth remembering before anyone repeats the claim.

## Уровень 0 и первый срез уровня 1 — приземлено

Оператор сказал «поехали по плану». Четыре коммита в `main`, каждый прошёл три ворот,
450 тестов зелёные.

`4c302d8` — **реестр**. Атомарная запись через temp+rename, как в `spending.ts`. Нечитаемый файл
больше не превращается молча в пустой флот: он откладывается в `<path>.unreadable` и об этом
кричат. Сбой записи больше не выбрасывает исключение из `setStatus` в то, что в этот момент
выполнялось. Два теста из трёх краснеют на старом коде.

`9cf1dac` — **отказ, за которым никто не следил**. Обработчик `unhandledRejection` там, где его
не было вообще. Не чинит отклонения, а делает их слышными; флот при этом продолжает работать
намеренно — рестарт всё равно не чинил состояние, из-за которого случился. Вынесено в
`unobserved-failure.ts` отдельной функцией, потому что `start.ts` из теста не импортируется, а
сигнализация, которую никто никогда не видел сработавшей, стоит мало.

`fdf7fa5` — **устаревшая точка старта**. Не двигаем чужую ветку, а режем новую от правильного
коммита: `whereANewBranchShouldStart` делает fetch и берёт upstream только если в чекауте нет
ничего, чего нет на сервере. Разошедшийся, или с локальной работой, или вовсе без remote —
остаётся при своём. Проверено на настоящих репозиториях, а не на подделке. У `Worktrees` тестов
не было вообще — класс был в списке тридцати двух экспортов, не упоминаемых в тестах ни разу.

`be1e4f6` — **компакт**. `compactionRequested` был флагом, снимавшимся только на событии
`compacted`; отменённый компакт оставлял sid навсегда и выключал автокомпакт до конца жизни
сессии. Теперь это не флаг, а момент: если за две минуты ничего не пришло, запрос забывается и
следующий опрос спросит снова. Ничто не обязано прийти, чтобы механизм ожил, — свойства, которого
у множества не было. Плюс снимается на `clear()`.

Почему у компакта не было тестов — причина механическая, не небрежность: весь путь от
`pollLiveTokens` вниз был приватным, единственный вызывающий — `setInterval`, шва не существовало.
Теперь `pollLiveTokens` публичный и принимает время, ровно как лимитный код в этом же файле.

## Что измерено вместо сомнительного числа

Воркер F доложил «28 из 31 обещания README покрыто». Проверить это я не мог — сам же попросил не
перечислять покрытые. Померил механически: **88 экспортируемых функций и классов, 32 не
упоминаются в `tests/` ни разу.** Хуже всего `capacity.ts` (3 из 3), `cli-binary.ts` (2 из 2),
`workspace.ts` (9 из 23). Это число можно проверить за секунды, в отличие от первого.

## Открыто у оператора

`c739c383` — `mayWriteInsideOrchestrator` объявлен в `RoleProfile`, входит в отпечаток роли, и
применяется только к ролям с префиксом `__`. Обычная роль получает перезапуск и ноль прав, молча.
Чинить выдачей прав или громким отказом — решение про то, что агенту позволено, то есть не моё.
Рекомендовал громкий отказ.

## Уровень 2.1 — механизм построен и приземлён (`47163f0`)

Оператор: «хватит почти на всё, не тормозим, этот рефакторинг очень нужная штука». Взял корень.

**Уточнение к дизайну, которого не было днём.** Времён жизни не два, а **три, и они вложены**:
процесс ⊂ разговор ⊂ сессия. Процесс кончается при каждой парковке. Разговор — при `clear()`, и
sid его переживает. Сессия — при закрытии. Уборка была организована вокруг первого (всё идёт
через `detachFromItsProcess`), поэтому принадлежащее второму переживало разговор, про который оно
было, и советовало следующему.

`src/session-state.ts`: коллекция запрашивается по имени и по времени жизни, три окончания
вложены. Переведено тринадцать. Два оставлены и поимённо названы в тесте, который иначе бы их
поймал: `roleFingerprints` ключуется ролью, `stoppedOnPurpose` — id задачи.

**Главное здесь не механизм, а тест.** Добавить четырнадцатую коллекцию по sid, не объявив время
жизни, теперь роняет набор — вместо того чтобы дождаться и стать четвёртым критом. Дисциплина,
которую нельзя забыть, лучше дисциплины, которую все собирались соблюдать.

**Что доказано и что нет, честно.** Механизм проверен напрямую: каждое окончание забывает ровно
свою область и вложенные, по одной сессии. Проводка — подтверждена, но не доказана: 465 тестов
прошли без изменений, и отдельный тест показывает, что все тринадцать приходят из `SessionState`.
Существующие ручные удаления оставлены намеренно — коммит добавляет власть, не убирая поведение.
Убрать лишние — следующий шаг, и именно он умеет сломать.

Сквозной тест на компакт проходит и против `00901e3`, и в сообщении коммита это сказано: то поле
я снял руками предыдущим коммитом. Он сторожит, а не доказывает.

**Вне механизма остались** `humanOutbox` и `reportsHeldUntilUnfreeze` — это массивы записей,
несущих sid, а не коллекции по ключу sid. Оба держат доставки, предназначенные разговору, который
`clear()` может выбросить. Следующий срез, не недосмотр.

## Компакт не сработал ни разу за всю жизнь флота — измерено

Попросил себе компакт в 16:00. Проверил, как сам же предписал: 50% до, **52% после**. Не сработал.

Активность даёт полную последовательность, и она снимает неопределённость, которая была в
карточке #55:

    16:00:36  message  supervisor → root-5cf1dd86   3.7k chars   ← /compact + инструкции
    16:00:46  report   root-5cf1dd86  ok, 1.2k
    16:02:26  report   root-5cf1dd86  ok, 0 chars   ← подпись

**Сообщение дошло.** Сессия потратила на него ход. Событие `compacted` не пришло, контекст не
упал. То есть отказ на стороне CLI, а не в доставке.

И сильнее: `grep compact` по всему `activity.log`, за вычетом перечней инструментов, даёт **ноль**
строк. За всю жизнь этого флота не произошло ни одного компакта. То же самое я наблюдал в прошлом
флоте. Два флота, ноль успешных компактов.

Из чего следует вывод, которого не было в плане: **`compactAtPercent` может не работать вообще
никогда**, и вся модель стоимости флота опирается на механизм, который ни разу не наблюдали
работающим. Это не «иногда отменяется» — это «не видели, чтобы срабатывало».

Гипотеза, проверяемая одним вызовом: дело в длине. Инструкция была 3.7 КБ. Проверяю коротким
`self_compact` — если сработает, причина в размере и лечится усечением или другим способом
передачи; если нет, размер ни при чём и надо копать глубже.

Проверка бесплатна в обе стороны: сработает — я получу нужный мне компакт, не сработает — узнаю,
что гипотеза неверна.

## Housekeeping left behind

  The workspace repo has four local commits and **no `origin/main`** — my push to
  `ye666w/silicyte-solo-dev` was refused by the harness. Everything is committed; nothing is
  backed up. Worth raising.
  `/Users/photon/projects/silicyte/dev-team-archive/` — 736K of the old fleet's state, mode 700,
  holding an `integrations.json` with a live Telegram token. One `rm -rf` from gone if the
  operator wants it gone; the solo fleet has no integrations configured at all yet.
  Two branches kept in the product on purpose: `silicyte/core-5cfba97e` and
  `silicyte/panel-d827bc59`, three unique commits each, from a design older than the nine roles.
  They look superseded by `spending.ts` and the rate-limit panel work, but that was not proved,
  so they were not deleted. Proving it is a good first worker task.

## Compaction: cause found, and two of my own claims retracted

Level 1 of the plan ("establish why the CLI cancels a compaction") is answered and fixed. It
is not length, and the mechanism was never dead.

Measured against worker-f6444092 held near 30% context. Three rounds, each attributable
because no human typed anywhere near it:

    /compact                                 8 chars    fired
    /compact <57-character single line>     66 chars    fired
    /compact <3.5 KB, single line>        3.5k chars    fired
    /compact <3.7 KB, paragraph breaks>   3.7k chars    cancelled

A slash command ends at the first newline. Everything after it stops being part of the
command, and the compaction is cancelled in silence. Landed as 38579d9: whitespace is
collapsed, so a long instruction arrives whole and on one line. Deliberately not a length cap
— the test asserts the fortieth line survives, so nobody later "repairs" this by truncating.

Two things I had written down were wrong, both in the alarming direction:

  - "No compacted event in either fleet's log, so the product's cost model rests on a
    mechanism nobody has observed run." The threshold path calls queueCompaction with no
    instructions — exactly the case that always fires. Nothing had ever reached 75%. Zero
    events meant zero sessions at the threshold, not a dead mechanism.
  - "The 0-char report is the signature of a cancellation." It follows a successful compaction
    too. The only discriminator is the `compact` line in .silicyte/activity.log.

The method is the reusable part. My own two data points were worthless: the operator typed
/compact 25 seconds after my own attempt and exactly one compaction fired, so neither could be
attributed. A worker is an isolated subject with nobody else typing into it, and three clean
rounds cost one Haiku session. When a measurement about my own behaviour is confounded by the
operator sharing the channel, move the experiment to a worker.

## Refactor: closeOne (2782d36)

Took out the three hand-written cleanup lines in closeOne that sat immediately after
theSessionEnded, which ends all three lifetimes, with nothing in between that reads them. The
rest stay and the commit message says why for each: recoverFromFailedTurn has no lifecycle
event behind it at all, detachFromItsProcess is the synchronous half of parking and the
fan-out only catches up when the drain's finally runs, and the pair in
thatProcessIsDoneReporting belong to scopes a theProcessEnded does not reach by design.

SessionState gained whatIsStillHeldAbout(sid). That turns "nothing was left behind" from a
claim into an assertion that names the offending collection when it fails. tests/forgetting.

Writing that test corrected me once, which is the argument for having written it: after
clear() the parking flag is still set, and I had expected it gone. clear ends the conversation
and then parks, so the flag is the sequence working rather than state left behind.

471 tests, lint and typecheck green. Landed: 2782d36, 38579d9.
